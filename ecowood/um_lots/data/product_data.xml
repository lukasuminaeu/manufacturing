<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Updadate decimal precision to 3 digits so we can update units of measure -->
        <record forcecreate="True" id="product.decimal_volume" model="decimal.precision">
            <field name="name">Volume</field>
            <field name="digits">3</field>
        </record>
       
    </data>
</odoo>
